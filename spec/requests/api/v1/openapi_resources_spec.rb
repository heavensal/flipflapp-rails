# frozen_string_literal: true

require "swagger_helper"

# Former getCurrentUser OpenAPI path removed — owned by users_me_spec.rb.
RSpec.describe "Api::V1 OpenAPI resources placeholder", type: :request do
  it "keeps the file for historical CI path expectations" do
    expect(true).to be(true)
  end
end
