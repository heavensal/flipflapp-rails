  <!-- Team A -->
  <div class="rounded-2xl bg-team-a/90 backdrop-blur-md shadow-md p-3 flex flex-col gap-2">
    <h2 class="text-center font-bold text-white">
      <%= team_1.name %>
    </h2>

    <div class="flex flex-col gap-1">
      <% if team_1.event_participants.empty? %>
        <p class="text-center text-gray-400">Aucun joueur dans cette équipe</p>
      <% end %>
      <% team_1.event_participants.each do |event_participant| %>
        <%= render 'event_participants/one_player_team_1_card', event_participant: event_participant %>
      <% end %>
    </div>

    <% unless team_1.in_this_team?(current_user) %>
      <div class="mt-auto">
        <%= render 'event_participants/form_btn', event: event, team: team_1 %>
      </div>
    <% end %>
  </div>
  <!-- End Team A -->


<%# render 'event_teams/components/team_1', event: @event, team_1: @team_1 %>
