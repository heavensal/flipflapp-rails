<%= link_to t("events.destroy.action"),
      event_path(event),
      data: {
        turbo_method: :delete,
        turbo_confirm: t("events.destroy.confirm")
      },
      class: "shadow-md bg-gradient-to-r from-red-600 to-red-800 text-white py-2 px-4 rounded-2xl active:scale-95 transition" %>
