<%= link_to t("events.actions.destroy"),
      event_path(event),
      data: {
        turbo_method: :delete,
        turbo_confirm: t("events.confirms.destroy")
      },
      class: "btn-danger" %>
