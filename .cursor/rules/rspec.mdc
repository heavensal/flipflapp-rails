---
description: Model specs and TDD
globs: spec/**/*
alwaysApply: false
---

# Specs

Read [docs/TESTING.md](../../docs/TESTING.md), [docs/DOMAIN.md](../../docs/DOMAIN.md).

- Model and request specs only in `spec/models/` and `spec/requests/` unless policy changes.
- Factory Bot; no fixtures; no `pending` examples.
- Files under 150 lines.
- Do not run `rspec` unless explicitly requested.
