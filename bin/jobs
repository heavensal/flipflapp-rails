#!/usr/bin/env ruby
# frozen_string_literal: true

require "rubygems"
require "bundler/setup"

config = File.expand_path("../config/sidekiq.yml", __dir__)
ARGV.unshift("-C", config) unless ARGV.include?("-C")

load Gem.bin_path("sidekiq", "sidekiq")
